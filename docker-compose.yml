version: "3.9"
   
services:
  app:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/backend
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    env_file:
      - ./.env.dev
  db:
    image: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env.dev
  redis:
    image: redis:alpine
    command: redis-server
    ports: 
      - "6379:6379"
    volumes:
      - $PWD/redis-data:/var/lib/redis
      - $PWD/redis.conf:/usr/local/etc/redis/redis.conf
    environment:
      - REDIS_REPLICATION_MODE=master
  celery:
    build: .
    command: celery --app=stx_training_program worker -l INFO
    user: nobody
    links:
      - redis
    volumes:
      - .:/backend
    depends_on:
      - app
    env_file:
      - ./.env.dev
volumes:
  postgres_data:

    #celery --app=stx_training_program worker -l INFO